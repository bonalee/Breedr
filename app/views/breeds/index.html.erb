<!-- Page Title -->
<div class="section section-breadcrumbs">
  <div class="container">
    <div class="row">
      <div class="col-sm-12">
        <h1>Breeds</h1>
      </div>
    </div>
  </div>
</div>

<% @breeds.each do |breed| %>
  <h3><a href='/breeds/<%= breed.id %>'><%= breed.name %></a></h3>
<% end %>